# Server the mobile apps talk to for jobs, checkout, payment verification and
# email. Point this at the deployed site once it's live on Vercel.
NEXT_PUBLIC_API_BASE=https://getrehired.vercel.app

# TEMPORARY: route checkout to the bundled mock page and accept the dev_mock
# checkout id without server verification. Remove once the site is deployed
# and real Polar checkout is wired up.
NEXT_PUBLIC_PAYMENT_MOCK=1
